%p= link_to "Home", root_path
%h1= @gallery.name

%br

%ul.thumbnails
  = render partial: @paintings
%hr

= form_for [@gallery, @gallery.paintings.new] do |f|
  %p
    = f.file_field :image, multiple: true
    = f.submit class: "btn btn-info"
